/* ═══════════════════════════════════════════════════════════════
   site-config.js — 이 파일 하나만 수정하면 됩니다!
   ═══════════════════════════════════════════════════════════════

   아래 따옴표 안의 내용을 선생님 상황에 맞게 바꾸고 저장하세요.
   나머지 모든 페이지(첫 화면, SQL Playground, SQL Tutorial,
   ERD Playground)에 자동으로 반영됩니다.

   ※ 규칙: 따옴표(")와 쉼표(,)는 지우지 마세요.
   ═══════════════════════════════════════════════════════════════ */

window.SITE_CONFIG = {

  /* ── 1. 기본 정보 ──────────────────────────────────────────── */

  // 과목/사이트 이름 (첫 화면 큰 제목, 브라우저 탭 제목에 사용)
  courseTitle: "데이터베이스 SQL 실습",

  // 선생님 성함
  teacherName: "김쌔미앙TV",

  // 학교(소속) 이름
  schoolName: "교육부 경기도교육청 전문교과교사연구회",

  // 학기 표시
  semester: "비저장 데이터베이스 실습 도구",

// 첫 화면 제목 아래에 나오는 소개 문장
  siteSubtitle: '설치 없이 브라우저에서 바로 실행하는 SQL·ERD 실습 도구입니다. 모든 작업은 내 컴퓨터 안에서만 처리되고, 데이터가 외부로 전송되지 않습니다.',

  // 페이지 맨 아래(푸터)에 함께 표시할 안내 문구
  footerNote: "실습 데이터(김쌔미앙TV)는 가상으로 생성된 교육용 데이터입니다.",


  /* ── 2. 색상 ──────────────────────────────────────────────────
     6자리 색상 코드(#RRGGBB)로 적습니다.
     색상 코드는 구글에 "color picker"를 검색하면 쉽게 고를 수 있습니다.

     base  : 사이트의 기본 색 (제목, 버튼, 상단 배경 등)
     point : 강조 색 (링크 강조, 테두리, 포인트 요소)
  ──────────────────────────────────────────────────────────── */오전 9:18 2026-07-29

  colors: {
    base:  "#002D56",   // 예: 남색 "#1D3A8A", 초록 "#14532D", 자주 "#6B1D45"
    point: "#8D7150",   // 예: 주황 "#C2410C", 청록 "#0F766E", 금색 "#A16207"

    /* 아래는 고급 설정입니다. 보통은 그대로 두면 됩니다.
       바꾸고 싶으면 줄 앞의 // 를 지우고 색상 코드를 수정하세요. */
    // ink:   "#1A1A1A",   // 본문 글자색
    // gray:  "#6B7280",   // 보조 글자색
    // light: "#F5F7FA",   // 옅은 배경색
    // line:  "#D9E0E8",   // 구분선 색
  },

};
